package gov.df.fap.service.gl.core.interfaces;

import java.sql.PreparedStatement;

/**
 * PreparedStatement handler, implemence need to supply all operation with PreparedStatement.
 * include execute update, query and converte ResultSet to the Object needed,after all return
 * the result Object.
 * @author 
 * @version 
 */
public interface PreparedStatementHandler {

	public Object handler(PreparedStatement ps);
}
